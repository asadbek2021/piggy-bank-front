export interface IAccounts {
  _id: string;
  user_id: string;
  title: string;
  description: string;
  currency: string;
  balance: string;
  createdAt?: string;
  updatedAt?: string;
  sign: string;
}
