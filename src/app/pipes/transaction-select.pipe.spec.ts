import { TransactionSelectPipe } from './transaction-select.pipe';

describe('TransactionSelectPipe', () => {
  it('create an instance', () => {
    const pipe = new TransactionSelectPipe();
    expect(pipe).toBeTruthy();
  });
});
